<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chatapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChatApp">
        <activity android:name=".TakipcileriGoster"></activity>
        <activity android:name=".TakipEdilenleriGoster" />
        <activity android:name=".ForumYorumYap" />
        <activity android:name=".HomePageActivity" />
        <activity android:name=".TakipActivity" />
        <activity android:name=".ForumActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".profilGoster" />
        <activity android:name=".kullaniciEkran" />
        <activity android:name=".profilEkran" />
        <activity android:name=".AnaEkran" />
        <activity android:name=".GirisYap">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>